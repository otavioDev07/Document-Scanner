enum ScannerCameraState {
  uninitialized,
  initializing,
  ready,
  starting,
  previewing,
  capturing,
  processing,
  paused,
  error,
  disposed,
}

enum ScannerFlashMode { off, auto, on, torch }
